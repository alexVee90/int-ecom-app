<%- include('partials/header') %> 
<div class="hero">
  <% if(err.msg === 'Product Not Found') { %>
    <h3>SOLD OUT!</h3>
    <p>(in fact there are no / no more products under this subcategory)</p>
  <% } else { %>
    <h2>ERROR</h2>
    <h3>STATUS: <%= err.status %></h3>
    <h3><%= err.msg %></h3>
  <% } %>
</div>
<%- include('partials/footer') %>